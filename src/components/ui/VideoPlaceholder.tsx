export const VideoPlaceholder = ({ children }: { children: React.ReactElement }) => (
    <div className="rounded-xl w-auto h-[500px] mx-auto aspect-video bg-black flex items-center justify-center text-white">
        {children}
    </div>
);
